# Unix sockets are available only inside the database container for administration.
local all all trust
hostnossl all all 0.0.0.0/0 reject
hostnossl all all ::/0 reject
hostssl thebirthdates birthday_app 0.0.0.0/0 scram-sha-256
hostssl thebirthdates birthday_app ::/0 scram-sha-256
host all all 0.0.0.0/0 reject
host all all ::/0 reject
